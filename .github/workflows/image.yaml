name: Build and Push Docker Image

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/palacios427/k8s-custom-nginx

permissions:
  contents: read
  packages: write

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

kubeadm join 192.168.56.101:6443 --token ojd9zl.1itobn2v4u13xrh6 \
	--discovery-token-ca-cert-hash sha256:7f4b596ad721f6a667b3fa0d2180d491aef9c516bec06817665b72b8789498ac
