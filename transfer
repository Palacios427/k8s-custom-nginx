sudo kubeadm reset -f
rm -rf $HOME/.kube
sudo rm -rf /etc/kubernetes/manifests/
sudo rm -rf /etc/kubernetes/pki/
sudo rm -rf /var/lib/calico



echo "KUBELET_EXTRA_ARGS=--node-ip=10.74.47.215" | sudo tee /etc/default/kubelet
sudo systemctl restart kubelet
sudo systemctl restart containerd



sudo swapoff -a
sudo kubeadm init --pod-network-cidr=172.16.0.0/16 --cri-socket=unix:///run/containerd/containerd.sock --apiserver-advertise-address=10.74.47.215



mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
kubectl taint nodes --all node-role.kubernetes.io/control-plane-



kubectl get nodes -o wide
